Mode: Dev

Read agent.md. Then implement Step 1 only.

Create vanish/reporting/webapp/database.py with SQLAlchemy engine reading DATABASE_URL from environment, SessionLocal factory, Base declarative base, and get_db dependency that yields a session and closes it.

Create vanish/reporting/webapp/models.py with two models. First: JobEvidence (table job_evidence) with columns id (integer PK autoincrement), job_id (string not null), job_status (string nullable), top_parent_flow (string nullable), task_name (string not null), task_flow (string nullable), task_status (string nullable), issued_by_flow (string nullable), with_items (text nullable), flow_payloads (JSON nullable), job_updated_at (datetime nullable), created_at (datetime default utcnow). Unique constraint on (job_id, task_name). Second: AnalysisCheckpoint (table analysis_checkpoint) with columns id (integer PK autoincrement), checkpoint_key (string unique not null), last_job_updated_at (datetime not null), updated_at (datetime default utcnow).

In webapp/__init__.py add startup event that calls Base.metadata.create_all(bind=engine). Add GET /health endpoint that runs SELECT 1 and returns {"status": "ok", "database": "connected"}.

Comment every function and every non-obvious line. Commit when done.
